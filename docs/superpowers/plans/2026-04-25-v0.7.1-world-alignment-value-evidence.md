# v0.7.1 World Alignment Value Evidence Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add release-grade v0.7.1 world-alignment gate evidence and internal value metrics without claiming external validation.

**Architecture:** Batch 1 adds deterministic scorecard/reporting around existing `review_world_alignment` outputs. Batch 2 adds an internal metrics module that runs fixed fixtures for on/off ablation, misuse interception, temporal sensitivity, verdict diversity, and no-forced-enhancement distribution.

**Tech Stack:** Python 3.10+, PyYAML, unittest, existing KiU scripts and `src/kiu_pipeline/world_alignment.py`.

---

## Batch 1: Evidence Transparency Closure

### Task 1: Add Gate Evidence API

**Files:**
- Modify: `src/kiu_pipeline/world_alignment.py`
- Test: `tests/test_world_alignment.py`

- [ ] Add a function that aggregates multiple `review_world_alignment` outputs into a release gate scorecard with actual values, thresholds, pass/fail status, and evidence paths.
- [ ] Include mechanism run counts: need scoring, intervention levels, candidate arbitration, no-forced-enhancement, source-fit review, dilution-risk review, hallucination-risk review.
- [ ] Test that the scorecard passes for three generated sample bundles and fails when source pollution is injected.

### Task 2: Add Gate Evidence Script And Report

**Files:**
- Create: `scripts/report_world_alignment_gate.py`
- Create: `reports/2026-04-25-v0.7.1-world-alignment-gate-evidence.md`
- Modify: `backlog/board.yaml`

- [ ] Add a script that writes JSON/Markdown gate evidence from sample bundle review data.
- [ ] Add KIU-715 through KIU-718 to the backlog with acceptance criteria and evidence pointers.
- [ ] Generate the v0.7.1 gate evidence report and include the condition-dependent validation backlog reference.

### Task 3: Add Condition-Dependent Validation Backlog

**Files:**
- Create: `docs/condition-dependent-validation-backlog.md`
- Modify: `README.md` or `docs/2026-04-24-v0.7-world-alignment-charter.md` if a pointer is needed.

- [ ] Document external blind review, real user validation, live-web factual validation, and domain-expert validation.
- [ ] For each item, record trigger conditions, current non-blocking reason, and how it becomes a future release gate.

### Batch 1 Verification And Commit

- [ ] Run `PYTHONPATH=src python3 -m unittest tests.test_world_alignment`.
- [ ] Run `python3 -c "from pathlib import Path; import yaml; yaml.safe_load(Path('backlog/board.yaml').read_text()); print('backlog yaml ok')"`.
- [ ] Commit with `git commit -m "Add v0.7.1 world alignment gate evidence"`.

## Batch 2: Value Metrics And Ablation

### Task 4: Add Value Metrics API

**Files:**
- Modify: `src/kiu_pipeline/world_alignment.py` or create `src/kiu_pipeline/world_alignment_metrics.py`
- Test: `tests/test_world_alignment.py`

- [ ] Implement `alignment_usage_delta`, `misuse_intercept_rate`, `no_forced_enhancement_rate`, `verdict_diversity_score`, and `temporal_sensitivity_response` calculations.
- [ ] Keep metrics deterministic and fixture-based.
- [ ] Keep release gate and stretch target values explicit.

### Task 5: Add Value Metrics Script And Report

**Files:**
- Create: `scripts/report_world_alignment_value_metrics.py`
- Create: `reports/2026-04-25-v0.7.1-world-alignment-value-metrics.md`
- Modify: `backlog/board.yaml`

- [ ] Add KIU-719 through KIU-724 to backlog with release gates and stretch targets.
- [ ] Generate report with metric actuals, release gate status, stretch target status, and claim boundary.

### Batch 2 Verification And Commit

- [ ] Run `PYTHONPATH=src python3 -m unittest tests.test_world_alignment tests.test_proxy_usage tests.test_reference_benchmark`.
- [ ] Run full test suite before final v0.7.1 release prep.
- [ ] Commit with `git commit -m "Add v0.7.1 world alignment value metrics"`.
