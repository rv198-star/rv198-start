# KiU v0.4.2 Optimization Plan

## Goal

Turn the combined v0.4.1 review into a concrete v0.4.2 hardening pass:

- close release/documentation honesty gaps
- eliminate generated-summary drift
- fix the known story-only routing underfire
- keep v0.5 scope explicitly deferred instead of quietly dropped

## Task 1: Close Documentation and Release Hygiene

Modify:

- `CHANGELOG.md`
- `README.md`
- `docs/usage-guide.md`
- `docs/CONTRIBUTING.md`
- `docs/kiu-skill-spec-v0.4.md`
- `docs/2026-04-22-v0.4.1-assessment-and-v0.4.2-plan.md`

Acceptance:

- `0.3.1`, `0.4.0`, and `0.4.1` are recorded in the changelog
- the repo states clearly that `v0.4.x` is still the single-domain hardening line
- v0.4 public rules for example fixtures, production quality, and honest summaries are documented

## Task 2: Make Structured YAML the Truth for Generated Summaries

Modify:

- `src/kiu_pipeline/draft.py`
- `src/kiu_pipeline/render.py`
- `src/kiu_pipeline/mutate.py`
- `src/kiu_pipeline/preflight.py`

Acceptance:

- generated `Evaluation Summary` is rendered from `eval/summary.yaml`
- generated `Revision Summary` is rendered from `iterations/revisions.yaml`
- refinement mutation refreshes both markdown sections after quality changes
- preflight rejects any generated bundle where markdown drifts from structured docs

## Task 3: Fix Real-Consumption Routing Gap

Modify:

- `examples/fixtures/financial-statement-analysis.yaml`

Acceptance:

- `lock-onto-accounting-value` fires on the story-only adversarial pattern
- the same symbol is no longer placed in exclusions

## Task 4: Mark Manual v0.4 Content Upgrades Honestly

Modify:

- `bundles/poor-charlies-almanack-v0.1/skills/*/SKILL.md`
- `bundles/poor-charlies-almanack-v0.1/skills/*/iterations/revisions.yaml`

Acceptance:

- revision 4 entries explicitly say the v0.4 rewrite was a manual content upgrade
- the corresponding markdown summaries do not imply a loop-driven refinement run

## Task 5: Align Profile Inheritance Naming

Modify:

- `src/kiu_pipeline/profile_resolver.py`
- tests as needed

Acceptance:

- `inherits_from` resolves successfully
- `inherits` still works for older bundles
- conflicting fields fail loudly

## Verification

Run:

```bash
.venv/bin/python -m unittest tests/test_profile_resolver.py
.venv/bin/python -m unittest tests/test_pipeline.py
.venv/bin/python -m unittest tests/test_refiner.py
.venv/bin/python -m unittest tests/test_validator.py
.venv/bin/python scripts/validate_bundle.py bundles/poor-charlies-almanack-v0.1
```
