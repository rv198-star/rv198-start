# v0.6.6 Blind Review Pack Fix Evidence

Date: 2026-04-25

## Scope

v0.6.6 is a patch closure for the v0.6.5 review findings. It does not start v0.7 and does not treat the returned review of the defective v0.6.5 blind pack as valid external blind evidence.

## Fixed Review Findings

| Finding | Fix | Evidence |
| --- | --- | --- |
| Blind pack could compare real artifact vs placeholder | KiU generated bundle paths now resolve `bundle/skills/<skill_id>/SKILL.md`; pack summary reports `placeholder_count`. | `placeholder_count=0` in v0.6.6 Shiji pack |
| A/B side balance was not enforced | Hidden roles now use deterministic balanced assignment with `abs(a_kiu_count-b_kiu_count)<=1`. | v0.6.6 Shiji pack: `13/13` |
| Borrowed-value boundary missed character evaluation, viewpoint summary, workflow template requests | Common anti-conditions added to borrowed-value contracts and specific history/role contracts. | `tests/test_v066_review_fixes.py` |
| Weak source forms could outrank mechanism-dense chapters | `case_density_score` added to routing evidence and seed scoring to downrank weak source forms such as tables, divination lists, and genealogy-like sections. | `tests/test_v066_review_fixes.py` |
| Repeated generic I/Rationale template was only visible by manual review | Generated-run review now emits `rationale_template_collision` warnings. | `tests/test_v066_review_fixes.py` |
| `compatibility_regression.risk` remained unknown | v0.6 regression baseline was replayed and can be passed into benchmark scorecards. | `/tmp/kiu-v066-regression-baseline/reports/v0.6-regression-baseline.json` |

## Fresh Evidence

Blind review pack rebuild:

```bash
PYTHONPATH=src python3 scripts/build_blind_review_pack.py --benchmark-report /tmp/kiu-v065-closure-shiji/shiji-kiu-vs-cangjie-protocol-r3-split.json --output-dir /tmp/kiu-v066-blind-review-pack-shiji --review-id v066-shiji-cangjie-protocol-blind
```

Result:

```json
{
  "pair_count": 26,
  "placeholder_count": 0,
  "role_balance": {"a_kiu_count": 13, "b_kiu_count": 13, "absolute_delta": 0}
}
```

Compatibility baseline:

```bash
PYTHONPATH=src python3 scripts/run_v06_regression_baseline.py --output-root /tmp/kiu-v066-regression-baseline
```

Result: `executed=7`, `passed=7`, `failed=0`.

Benchmark with compatibility report:

```bash
PYTHONPATH=src python3 scripts/benchmark_reference_pack.py --kiu-bundle /tmp/kiu-v065-closure-shiji/sources/shiji-v065-closure/bundle --run-root /tmp/kiu-v065-closure-shiji/generated/shiji-v065-closure-source-v0.6/v065-closure-shiji --reference-pack /tmp/kiu-v065-closure-shiji/cangjie-protocol-r2 --comparison-scope same-source --compatibility-regression-report /tmp/kiu-v066-regression-baseline/reports/v0.6-regression-baseline.json --output /tmp/kiu-v066-shiji-benchmark.json
```

Result summary:

- `compatibility_regression_risk = pass`
- `cangjie_methodology_internal_100 = 100.0`
- `cangjie_methodology_external_blind_100 = 0.0`
- `cangjie_methodology_closure_100 = 0.0`
- `missing_capabilities = [blind_preference]`

## Verification

```bash
PYTHONPATH=src python3 -m unittest tests.test_blind_review_pack tests.test_v066_review_fixes tests.test_reference_benchmark
```

Result: `25 tests OK`.

```bash
PYTHONPATH=src python3 -m unittest discover -s tests -p 'test_*.py'
```

Result: `190 tests OK` with the expected `autonomous_refiner` deprecation warning.

## Remaining Gate

The valid external blind review has not been returned yet. v0.6.6 ships a corrected review pack so reviewers can produce valid evidence; it does not unlock `external_blind_100` by self-scoring.
