# v0.6.7 Boundary Rendering and Decoy Evidence

Date: 2026-04-25

## Scope

v0.6.7 closes the v0.6.6 review tail: the upgraded borrowed-value anti-condition contracts must be visible in regenerated `SKILL.md` artifacts and in the public blind-review pack, not only in contract-unit tests.

This remains a v0.6.x infrastructure/content-closure patch. It does not claim external blind closure and does not start v0.7 world-alignment work.

## Fixed Review Findings

| Finding | Fix | Evidence |
| --- | --- | --- |
| v0.6.6 blind pack reused v0.6.5 generated Shiji artifacts, so new anti-conditions were not reviewer-visible. | Reran Shiji and Mao raw-book no-seed closure generation with the current v0.6.7 code. | `/tmp/kiu-v067-closure-shiji`, `/tmp/kiu-v067-closure-mao` |
| Public reviewer excerpts could truncate the boundary section behind long contract text. | Blind pack excerpts now front-load boundary/anti-misuse windows before contract/method/rationale excerpts. | `tests.test_blind_review_pack...test_review_excerpt_surfaces_boundary_terms_before_long_contract` |
| KIU-685 needed end-to-end decoy validation, not contract string scanning. | Same-scenario usage evaluation now recognizes explicit no-fire anti-condition matches and requires p010/p020/p023-style decoys to pass. | `tests.test_reference_benchmark...test_same_scenario_decoys_pass_when_explicit_anti_conditions_match` |

## Fresh Generation Evidence

Shiji raw-book no-seed rerun:

```bash
PYTHONPATH=src python3 scripts/run_book_pipeline.py --input examples/shiji_md --bundle-id shiji-v067-closure-source-v0.6 --source-id shiji-v067-closure --run-id v067-closure-shiji --output-root /tmp/kiu-v067-closure-shiji --deterministic-pass heuristic-extractors --drafting-mode deterministic --inherits-from default --title "史记" --max-chars 1200
```

Result: generated bundle at `/tmp/kiu-v067-closure-shiji/generated/shiji-v067-closure-source-v0.6/v067-closure-shiji/bundle`.

Mao raw-book no-seed rerun:

```bash
PYTHONPATH=src python3 scripts/run_book_pipeline.py --input examples/MaoZeDongAnthology-master --bundle-id mao-v067-closure-source-v0.6 --source-id mao-v067-closure --run-id v067-closure-mao --output-root /tmp/kiu-v067-closure-mao --deterministic-pass heuristic-extractors --drafting-mode deterministic --inherits-from default --title "毛泽东选集" --max-chars 1200
```

Result: generated bundle at `/tmp/kiu-v067-closure-mao/generated/mao-v067-closure-source-v0.6/v067-closure-mao/bundle`.

Boundary propagation check:

- Shiji generated judgment skills `historical-analogy-transfer-gate`, `historical-case-consequence-judgment`, and `role-boundary-before-action` all include `pure_character_evaluation_request`, `pure_viewpoint_summary_request`, and `mechanical_workflow_template_request` in `SKILL.md`.
- Mao generated borrowed-value skills include the same three anti-condition symbols; workflow-gateway remains excluded because it is a router skill.

## Corrected Blind Review Pack

Built from the v0.6.7 Shiji rerun and v0.6.7 same-source benchmark:

```bash
PYTHONPATH=src python3 scripts/build_blind_review_pack.py --benchmark-report /tmp/kiu-v067-shiji-benchmark.json --output-dir /tmp/kiu-v067-blind-review-pack-shiji --review-id v067-shiji-control-style-B-blind
```

Result:

```json
{
  "pair_count": 26,
  "placeholder_count": 0,
  "role_balance": {"a_kiu_count": 13, "b_kiu_count": 13, "absolute_delta": 0}
}
```

Reviewer-visible `option_b` decoy check:

- `p009` option_b contains all three new anti-condition symbols.
- `p010` option_b contains all three new anti-condition symbols.
- `p020` option_b contains all three new anti-condition symbols.

The public pack is committed under `reports/blind-review-packs/v0.6.7-shiji-control-style-B/`. The private unblind key remains only under `/tmp/kiu-v067-blind-review-pack-shiji/private-unblind-key.json`.

## Benchmark and Regression Evidence

Compatibility baseline replay:

```bash
PYTHONPATH=src python3 scripts/run_v06_regression_baseline.py --output-root /tmp/kiu-v067-regression-baseline
```

Result: `executed=7`, `passed=7`, `failed=0`.

Same-source benchmark:

```bash
PYTHONPATH=src python3 scripts/benchmark_reference_pack.py --kiu-bundle /tmp/kiu-v067-closure-shiji/sources/shiji-v067-closure/bundle --run-root /tmp/kiu-v067-closure-shiji/generated/shiji-v067-closure-source-v0.6/v067-closure-shiji --reference-pack /tmp/kiu-v067-closure-shiji/control-style-B-r1 --comparison-scope same-source --compatibility-regression-report /tmp/kiu-v067-regression-baseline/reports/v0.6-regression-baseline.json --output /tmp/kiu-v067-shiji-benchmark.json
```

Summary:

- `kiu_generated_skill_count = 3`
- `reference_skill_count = 2`
- `matched_pair_count = 2`
- `scenario_count = 26`
- `cangjie_methodology_internal_100 = 100.0`
- `cangjie_methodology_external_blind_100 = 0.0`
- `cangjie_methodology_closure_100 = 0.0`
- `compatibility_regression_risk = pass`

## Remaining Gate

External blind review is still not returned. `external_blind_100` and `closure_100` remain `0.0` until auditors review the corrected v0.6.7 pack and return valid blind-preference evidence.

## Verification

```bash
PYTHONPATH=src python3 -m unittest tests.test_blind_review_pack tests.test_v066_review_fixes tests.test_reference_benchmark
```

Result: `27 tests OK`.

```bash
PYTHONPATH=src python3 -m unittest discover -s tests -p 'test_*.py'
```

Result: `192 tests OK` with the expected `autonomous_refiner` deprecation warning.

```bash
python3 -m build --no-isolation
```

Result: `kiu-0.6.7.tar.gz` and `kiu-0.6.7-py3-none-any.whl` built successfully.

Install smoke: `kiu_pipeline.blind_review_pack`, `kiu_pipeline.reference_benchmark`, and `kiu_validator.core` import successfully from the v0.6.7 wheel.
