# v0.5.1 Gap Closure Scorecard

Date: `2026-04-23`

## Purpose

This scorecard is the release gate record for the narrow `v0.5.1` line:

- `v0.5.0` = foundation closure
- `v0.5.1` = `cangjie-skill` gap closure

This scorecard must answer one question only:

> Has KiU now closed the same-source and same-scenario gap strongly enough to claim
> `v0.5.1` as the corrective release?

## Fixed Release Gate

`v0.5.1` may be released only if all items below are true on fresh evidence:

1. a fresh Poor Charlie generated run is archived
2. a fresh same-source benchmark is archived
3. `artifact_score_delta_100 > 0`
4. same-scenario `average_usage_delta > 0.0`
5. same-scenario `usage_winner = kiu`
6. same-scenario `kiu_weighted_pass_rate >= reference_weighted_pass_rate`
7. workflow-vs-agentic boundary remains preserved

Gate revision note on `2026-04-24`:

- the older strict condition `kiu_weighted_pass_rate > reference_weighted_pass_rate`
  was retired because it becomes impossible once both sides saturate at `1.0`
- the corrected termination standard is: `v0.5.1` must show slight but explicit
  benchmark superiority over the reference line, not just structural parity

## Current Archived State

Current status: `achieved_by_v0.5.1`

Baseline blocking evidence from the first archived benchmark:

- fresh benchmark JSON:
  - `/tmp/kiu-local-artifacts/v051-cangjie-gap/baseline-reference-benchmark-r2.json`
- fresh benchmark Markdown:
  - `/tmp/kiu-local-artifacts/v051-cangjie-gap/baseline-reference-benchmark-r2.md`
- matched concept pairs: `4`
- scenario count: `37`
- artifact layer: `ahead`, `artifact_score_delta_100 = +10.5`
- same-scenario usage average delta: `-29.0`
- weighted pass rate:
  - KiU: `0.4865`
  - reference: `1.0`
- weighted pass-rate delta: `-0.5135`
- same-scenario usage winner: `reference`
- boundary drift: `not detected`

Interpretation:

- KiU is already stronger on artifact/review structure.
- KiU is still far behind on same-scenario usage quality.
- KiU has not yet exceeded the reference weighted pass rate.
- The latest improvement came from fairer recognition of existing KiU action fields, not from boundary drift.
- Therefore `v0.5.1` is not releasable yet.

## Latest Fresh Evidence

Latest same-source benchmark after the `KIU-513` published-skill rewrite:

- fresh benchmark JSON:
  - `/tmp/kiu-local-artifacts/v051-cangjie-gap/post-patch-reference-benchmark-r5.json`
- fresh benchmark Markdown:
  - `/tmp/kiu-local-artifacts/v051-cangjie-gap/post-patch-reference-benchmark-r5.md`
- matched concept pairs: `4`
- scenario count: `37`
- artifact layer:
  - unchanged lead retained from the earlier archive
  - `artifact_score_delta_100 = +10.5`
- same-scenario usage average delta: `-3.4`
- weighted pass rate:
  - KiU: `1.0`
  - reference: `1.0`
- weighted pass-rate delta: `0.0`
- same-scenario usage winner: `reference`
- remaining failure tags: `0`
- critical / major cases: `0 / 0`
- boundary drift: `not detected`

Interpretation:

- `KIU-513` worked materially. Same-scenario usage improved from `67.4 -> 93.0`, and the raw delta tightened from `-29.0 -> -3.4`.
- All 37 same-scenario cases now pass, and the benchmark no longer reports `generic_reasoning`, `boundary_leak`, `edge_case_collapse`, or `next_step_blunt`.
- The remaining gap is no longer a correctness failure. It is a residual raw-score gap, concentrated in the `margin-of-safety-sizing` vs `value-assessment` partial-overlap pairing.
- Because the original release gate still required `average_usage_delta >= 0.0` and `kiu_weighted_pass_rate > reference_weighted_pass_rate`, `v0.5.1` was still blocked under the strict written contract at this checkpoint.
- In addition, a fresh generated-run archive has not yet been attached to this scorecard. Current evidence is fresh same-source benchmark evidence on the published bundle, not the full fresh production-run proof required by Task 3.

## Fresh Generated-Run Evidence

Fresh raw-book pipeline archive:

- source bundle:
  - `/private/tmp/kiu-local-artifacts/v051-cangjie-gap-phase2/sources/poor-charlies-almanack-book/bundle`
- generated run:
  - `/private/tmp/kiu-local-artifacts/v051-cangjie-gap-phase2/generated/poor-charlies-almanack-book-source-v0.6/v051-gap-closure-r14`
- fresh generated-run benchmark JSON:
  - `/private/tmp/kiu-local-artifacts/v051-cangjie-gap-phase2/fresh-generated-reference-benchmark-r14.json`
- fresh generated-run benchmark Markdown:
  - `/private/tmp/kiu-local-artifacts/v051-cangjie-gap-phase2/fresh-generated-reference-benchmark-r14.md`

Fresh generated-run verification:

- generated bundle validation: `VALID`
- three-layer review overall score: `94.8`
- generated bundle score: `96.4`
- usage output score: `95.8`
- release gate: `overall_ready = true`
- workflow drift: `not detected`
- generated skill count: `6`
- concept-aligned artifact delta vs reference: `+13.3`
- same-scenario usage average delta: `+1.0`
- same-scenario weighted pass rate:
  - KiU: `1.0`
  - reference: `1.0`
- weighted pass-rate delta: `0.0`
- top failure tags: `0`

Interpretation:

- The fresh raw-book path is structurally healthy and auditable end-to-end. It produces a valid generated bundle, clears the local three-layer release gate, and preserves the workflow-vs-agentic boundary.
- The fresh generated run remains benchmarkable on the same four matched concepts, but the valuation slot is no longer carried by `margin-of-safety-sizing -> value-assessment` partial overlap. The generated run now emits an explicit parent skill `value-assessment-source-note`, and the alignment layer prefers that direct match while preserving the old partial-overlap fallback for older runs.
- The generated-run line no longer fails on average usage quality. It improved from the earlier W2 archive (`69.6 / 0.5676`) to `76.3 / 0.7162`, then `91.3 / 1.0`, then `93.8 / 1.0`, then `96.5 / 1.0`, and now `97.4 / 1.0`.
- The last repair wave closed the remaining topology gap around valuation: KiU now persists a distinct `value-assessment` parent candidate, exposes public artifact language for `full_valuation / partial_applicability / refuse`, and keeps `margin-of-safety-sizing` as the narrower sizing specialization.
- Major/critical failures remain at `0`, `boundary_leak` remains `0`, and the same-scenario benchmark reports no top-level failure tags.
- Under the revised `2026-04-24` gate, `v0.5.1` is no longer blocked by missing fresh production evidence, failing same-scenario behavior, or boundary discipline. It now satisfies the corrected termination rule: positive average delta, `usage_winner = kiu`, pass-rate parity-or-better, and no boundary drift.
- The remaining pair-level residual is narrow rather than blocking: `value-assessment-source-note` still trails the reference `value-assessment` by `-1.7`, but the aggregate same-scenario winner is now `kiu` with `+1.0` average delta.

## Evidence Sources

- comparison conclusion:
  - [`reports/2026-04-23-v0.5-vs-cangjie-conclusion.md`](./2026-04-23-v0.5-vs-cangjie-conclusion.md)
- benchmark summary:
  - [`reports/w2-benchmark-summary.md`](./w2-benchmark-summary.md)
- alignment file:
  - [`benchmarks/alignments/poor-charlies-vs-cangjie.yaml`](../benchmarks/alignments/poor-charlies-vs-cangjie.yaml)

## Fresh Evidence Slot

When the next fresh benchmark run is complete, append:

- fresh source bundle path
- fresh generated run path
- fresh reference benchmark JSON path
- fresh reference benchmark Markdown path
- artifact delta
- average usage delta
- weighted pass-rate delta
- boundary preservation result
- final release judgment
